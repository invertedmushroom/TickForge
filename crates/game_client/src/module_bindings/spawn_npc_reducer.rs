// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct SpawnNpcArgs {
    pub pos_x: f32,
    pub pos_y: f32,
    pub pos_z: f32,
    pub max_hp: f32,
}

impl From<SpawnNpcArgs> for super::Reducer {
    fn from(args: SpawnNpcArgs) -> Self {
        Self::SpawnNpc {
            pos_x: args.pos_x,
            pos_y: args.pos_y,
            pos_z: args.pos_z,
            max_hp: args.max_hp,
        }
    }
}

impl __sdk::InModule for SpawnNpcArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `spawn_npc`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait spawn_npc {
    /// Request that the remote module invoke the reducer `spawn_npc` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`spawn_npc:spawn_npc_then`] to run a callback after the reducer completes.
    fn spawn_npc(&self, pos_x: f32, pos_y: f32, pos_z: f32, max_hp: f32) -> __sdk::Result<()> {
        self.spawn_npc_then(pos_x, pos_y, pos_z, max_hp, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `spawn_npc` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn spawn_npc_then(
        &self,
        pos_x: f32,
        pos_y: f32,
        pos_z: f32,
        max_hp: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl spawn_npc for super::RemoteReducers {
    fn spawn_npc_then(
        &self,
        pos_x: f32,
        pos_y: f32,
        pos_z: f32,
        max_hp: f32,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            SpawnNpcArgs {
                pos_x,
                pos_y,
                pos_z,
                max_hp,
            },
            callback,
        )
    }
}
